function addTwo(num: number): number {
  return num + 2;
}
function logIn(name: string, password: number, siPaid: boolean) {
  return {name, password, siPaid};
}

logIn("dwqdw", 2323, true);
let arr: number[] = [1, 2, 3, 4, 5, 5, 6];

